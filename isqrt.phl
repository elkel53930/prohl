vars x a b m
{
  x >= 0
}
a := 0;
b := x + 1;
while !(a + 1 == b)
inv a + 1 <= b & a * a <= x & x < b * b
do
  m := (a + b) div 2;
  if m * m <= x then
    a := m
  else
    b := m
  fi
od
{
  a * a <= x &
  x < (a + 1) * (a + 1)
}
